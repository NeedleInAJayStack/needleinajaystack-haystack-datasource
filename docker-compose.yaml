services:
  grafana:
    platform: 'linux/amd64'
    build:
      args:
        grafana_version: ${GRAFANA_VERSION:-latest}
    extends:
      file: .config/docker-compose-base.yaml
      service: grafana
